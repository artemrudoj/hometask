#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>
#include <sys/mman.h>
#include <unistd.h>





typedef struct _file_size
{
	int before_size;
	int after_size;
}file_size;


void* mapFileToMemory(int fd);
int compress(void *source, void *dest, int level , file_size *info);

